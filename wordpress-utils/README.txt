These are utilities which can be used to backup inactive users, delete inactive users, and restore deleted users.

Note that WordPress is highly dependent on which modules are installed, the naming convention of tables, etc. So this script may not work as-is for other
environments, though it should only require minor modifications in most cases.